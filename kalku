package main

import "fmt"

func main() {
	var target, a, total int

	fmt.Scan(&target)

	total = 0

	for total < target {
		fmt.Scan(&a)

		total = total + a

		fmt.Printf(" %d \n", a)
	}

	fmt.Printf("\n %d \n", total)
}
